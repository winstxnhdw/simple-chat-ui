name: CI

on:
  pull_request_target:
  push:
    branches: main
    paths:
      - .github/workflows/main.yml
      - pyproject.toml
      - uv.lock
      - '**.py'

jobs:
  python:
    uses: winstxnhdw/actions/.github/workflows/uv.yml@4d514e9cc5ee4206069ba3216a5751f08938e355 # main
    with:
      disable-test: true
